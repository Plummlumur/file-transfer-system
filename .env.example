# Database Configuration
DB_ROOT_PASSWORD=secureRootPassword123!
DB_NAME=file_transfer
DB_USER=fileuser
DB_PASSWORD=secureFileUserPassword456!

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-that-should-be-very-long-and-random-for-production

# Redis Configuration
REDIS_PASSWORD=secureRedisPassword789!

# LDAP Configuration (CONFIGURE THESE FOR YOUR ENVIRONMENT)
LDAP_URL=ldap://your-ldap-server:389
LDAP_BIND_DN=cn=service-account,ou=users,dc=company,dc=com
LDAP_BIND_PASSWORD=ldap-service-password
LDAP_SEARCH_BASE=ou=users,dc=company,dc=com

# SMTP Configuration (CONFIGURE THESE FOR YOUR ENVIRONMENT)
SMTP_HOST=smtp.company.com
SMTP_USER=noreply@company.com
SMTP_PASSWORD=smtp-password

# Application Configuration
FRONTEND_URL=http://localhost
NODE_ENV=development